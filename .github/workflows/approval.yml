name: Approval

on:
  # push:
  #   branches: [ main ]
  workflow_dispatch: 

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Build Application
        run: echo "Building..."

  # notify-request:
  #     runs-on: ubuntu-latest
  #     steps:
  #       - name: Slack Notification for Approval
  #         uses: rtCamp/action-slack-notify@v2
  #         env:
  #           SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
  #           SLACK_TITLE: "🚀 Deployment Approval Required"
  #           SLACK_MESSAGE: "New build is waiting for approval to PROD. [Review here](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
  #           SLACK_COLOR: "#EBB424" # Yellow for waiting

  deploy-to-prod:
    name: deploy-to-prod
    runs-on: ubuntu-latest
    needs: build                # Wait for build to finish
    environment:
      name: 'Production'   # This triggers the Manual Approval check
      url: https://jira.com/task/ABC-123
      # description: Please provide a business justification
      # name: ${{ startsWith(github.ref, 'refs/tags/v-prod') && 'Production' || 'Staging' }}
      # url: ${{ vars.APP_URL }}
    steps:
      - name: Deploy
        run: echo "Deploying to Production only after someone clicks APPROVE"
        # run: echo "Deploying to ${{ startsWith(github.ref, 'refs/tags/v-prod') && 'PROD' || 'STAGE' }}"
