name: Approval

on:
  # push:
  #   branches: [ main ]
  workflow_dispatch: 

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Build Application
        run: echo "Building..."

  deploy-to-prod:
    name: deploy-to-prod
    runs-on: ubuntu-latest
    needs: build                # Wait for build to finish
    environment: 'Production'   # This triggers the Manual Approval check
    steps:
      - name: Deploy
        run: echo "Deploying to Production only after someone clicks APPROVE"
